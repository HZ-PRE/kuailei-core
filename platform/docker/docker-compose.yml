version: '3.8'

services:
  sdm:
    image: ghcr.io/sdm/sdm-core:latest
    network_mode: host
    environment:
      CONFIG: "https://github.com/sdm/sdm-app/raw/refs/heads/main/test.configs/warp"
    volumes:
      - ./sdm.json:/sdm/sdm.json
    command: ["/opt/sdm.sh"]
